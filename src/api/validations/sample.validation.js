const Joi = require("joi");

module.exports = {
  /**
   * METHOD  /api/routeHere
   */
  // nameKey: {
  //   body: Joi.object({
  //     key1: Joi.string().required(),
  //     key2: Joi.string().required(),
  //   }),
  // },
};
